- name: Increase partition and filesystem
  hosts: all
  become: true
  tasks:
    - name: Increase partition
      command: growpart /dev/nvme0n1 1
      register: partition_grow
      changed_when: partition_grow.rc == 0
      failed_when: partition_grow.rc == 2
    - name: Increase filesystem
      command: resize2fs /dev/nvme0n1p1
      register: filesystem_grow
      changed_when: filesystem_grow.stdout != ""